#include "main.h"

/**
 * mul - checks main code
 * @a: take the first number
 * @b: takes the second number
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{
	int answer;

	answer = a * b;
	return (answer);
}
